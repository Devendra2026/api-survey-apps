# Review package Task 3

BASE: e34359ce88499431daffec53865440c494389718
HEAD: 5d473eaaf7d1432669d8ad8bae5a565cac78ea23

## Commits

5d473ea fix: harden Dokploy compose pins and Traefik exposure

## Stat

.superpowers/sdd/task-3-report.md | 48 +++++++++++++++++++++++++++++++++++++++ DEPLOYMENT.md | 4 ++-- deploy/docker-stack.swarm.yml | 14 +++++++----- docker-compose.dokploy.yml | 13 +++-------- docker-compose.yml | 4 ++-- docs/ops/dokploy-compose-setup.md | 2 +- docs/ops/go-live.md | 21 +++-------------- 7 files changed, 67 insertions(+), 39 deletions(-)

## Diff

````diff
diff --git a/.superpowers/sdd/task-3-report.md b/.superpowers/sdd/task-3-report.md new file mode 100644 index 0000000..390e693 --- /dev/null +++ b/.superpowers/sdd/task-3-report.md @@ -0,0 +1,48 @@ +# Task 3 Report: Docker Compose and Dokploy hardening + +## Status + +Complete. The Dokploy production stack keeps the required eight services (`postgres`, `redis`, `minio`, `minio-init`, `migrate`, `api`, `worker`, and `web`), uses Traefik as the only application ingress, preserves the migration gate, refreshes deployable MinIO image pins, and aligns Swarm Redis authentication. + +## Changes + +- Updated the MinIO server image to `minio/minio:RELEASE.2025-09-07T16-13-09Z`. +- Updated the MinIO client image to `minio/mc:RELEASE.2025-08-13T08-35-41Z`. +- Applied the same MinIO pins to Dokploy Compose, local Compose, and the secondary Swarm stack. +- Removed host `ports` mappings from Dokploy `api`, `worker`, and `web`. +- Kept `api` and `web` attached to `dokploy-network` with Traefik labels; kept `worker` internal. +- Updated deployment and go-live documentation to state that only Traefik ports `80`/`443` should be publicly exposed. +- Preserved the private Dokploy bucket initialization; no anonymous-download policy was added. +- Aligned the secondary Swarm Redis service with production authentication by requiring `REDIS_PASSWORD`, adding `--requirepass`, authenticating its health check, and updating API/worker Redis URLs. +- Made no application business-logic changes. + +## Migration gate confirmation + +- `migrate` still builds from `apps/api/Dockerfile`. +- `migrate` still runs `/app/apps/api/docker-entrypoint.migrate.sh`. +- Both `api` and `worker` still depend on `migrate` with `condition: service_completed_successfully`. + +## Validation + +- `docker manifest inspect minio/minio:RELEASE.2025-09-07T16-13-09Z` ΓÇö passed. +- `docker manifest inspect minio/mc:RELEASE.2025-08-13T08-35-41Z` ΓÇö passed. +- `docker compose -f docker-compose.dokploy.yml config --quiet` with validation-only secrets ΓÇö passed. +- `docker compose -f docker-compose.dokploy.yml config --services` ΓÇö returned exactly the eight required services. +- `docker compose -f docker-compose.yml config --quiet` ΓÇö passed. +- `docker compose -f deploy/docker-stack.swarm.yml config --quiet` with validation-only secrets and example env files ΓÇö passed. +- Dokploy Compose with required secrets unset ΓÇö failed closed as expected on `POSTGRES_PASSWORD`. +- `git diff --check` ΓÇö passed. + +## Concern and release-pin rationale + +The newest MinIO source release is `RELEASE.2025-10-15T17-29-55Z`, but MinIO's release notes require container users to build it themselves and Docker Hub has no manifest for that tag. The selected `RELEASE.2025-09-07T16-13-09Z` is therefore the newest official `minio/minio` release tag verified as directly deployable from Docker Hub. The latest published `minio/mc` image is `RELEASE.2025-08-13T08-35-41Z`; both images are from the same current release era and have multi-architecture manifests. + +## Files changed + +- `docker-compose.dokploy.yml` +- `docker-compose.yml` +- `deploy/docker-stack.swarm.yml` +- `DEPLOYMENT.md` +- `docs/ops/dokploy-compose-setup.md` +- `docs/ops/go-live.md` +- `.superpowers/sdd/task-3-report.md` diff --git a/DEPLOYMENT.md b/DEPLOYMENT.md index aa8f4f3..8e4f41f 100644 --- a/DEPLOYMENT.md +++ b/DEPLOYMENT.md @@ -70,30 +70,30 @@ pnpm turbo build --filter=web... --filter=api... --filter=worker...  There is **intentionally no root `Dockerfile`**. Do not create a single multi-process image for web+api+worker. Production is **Docker Compose only**.    | Setting       | Value                                                                                                 |  | ------------- | ----------------------------------------------------------------------------------------------------- |  | Build type    | **Docker Compose** (not Dockerfile / Nixpacks / Railpack)                                             |  | Compose file  | `docker-compose.dokploy.yml`                                                                          |  | Build context | repository root (`.`)                                                                                 |  | Dockerfiles   | Per service only: `apps/web/Dockerfile`, `apps/api/Dockerfile`, `apps/worker/Dockerfile`              | -| Ports         | web `3001ΓåÆ3000`, api `4000`, worker `4001`                                                            | +| Ports         | Traefik-only: web `3000`, api `4000`; worker `4001` remains internal                                  |  | Health        | web `/healthz`, api/worker `/live`                                                                    |  | Metrics       | api/worker `/metrics` (optional scrape; see [`docs/ops/observability.md`](docs/ops/observability.md)) |  | Infra images  | Postgres **17**, Redis **8**, MinIO (pinned RELEASE)                                                  |    1. Create a **Compose** application in Dokploy (build type = Docker Compose). See [`docs/ops/dokploy-compose-setup.md`](docs/ops/dokploy-compose-setup.md).  2. Compose file: [`docker-compose.dokploy.yml`](docker-compose.dokploy.yml).  3. Build context = **repository root** (each service `build.context: .`).  4. Dockerfile paths (via compose): `apps/web/Dockerfile`, `apps/api/Dockerfile`, `apps/worker/Dockerfile`.  5. Secrets in Dokploy **Environment** UI ΓÇö paste [`deploy/env/dokploy.compose.env.example`](deploy/env/dokploy.compose.env.example) (replace `REPLACE_ME_*`). Dokploy writes `.env`; compose loads it into migrate/api/worker/web.     **Interpolation-required (no defaults):** `POSTGRES_PASSWORD`, `REDIS_PASSWORD`, `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `DATABASE_URL`.     Also set Clerk keys and `NEXT_PUBLIC_*` ΓÇö see [`docs/ops/dokploy-env.md`](docs/ops/dokploy-env.md).  6. Domains: `admin.sdvedutech.in` ΓåÆ web:**3000**; `backend.sdvedutech.in` ΓåÆ api:**4000** (Traefik container ports). -7. Host ports (direct access / SG): web `3001ΓåÆ3000`, api `4000`, worker `4001`. +7. Traefik is the only ingress. Compose publishes no application ports on the host; worker `4001` stays internal.  8. Health: web `/healthz`, api/worker `/live`.  9. Deploy: migrate ΓåÆ api / worker / web healthy.    Web **build args** (Compose already wires these from the same Dokploy env): `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`.    **Postgres 16 ΓåÆ 17:** existing data volumes are not binary-compatible. Dump/restore or recreate the volume before first PG17 start.    --- diff --git a/deploy/docker-stack.swarm.yml b/deploy/docker-stack.swarm.yml index ce673bc..5b23bb4 100644 --- a/deploy/docker-stack.swarm.yml +++ b/deploy/docker-stack.swarm.yml @@ -39,33 +39,35 @@ services:          condition: on-failure          delay: 5s        placement:          constraints:            - node.role == manager      redis:      image: redis:8-alpine -    command: ["redis-server", "--appendonly", "yes", "--bind", "0.0.0.0"] +    environment: +      REDIS_PASSWORD: ${REDIS_PASSWORD:?set REDIS_PASSWORD} +    command: ["redis-server", "--appendonly", "yes", "--bind", "0.0.0.0", "--requirepass", "${REDIS_PASSWORD}"]      volumes:        - survey_redis_data:/data      networks:        - survey-internal      healthcheck: -      test: ["CMD", "redis-cli", "ping"] +      test: ["CMD-SHELL", 'REDISCLI_AUTH="$$REDIS_PASSWORD" redis-cli ping | grep -q PONG']        interval: 5s        timeout: 3s        retries: 10      deploy:        replicas: 1        restart_policy:          condition: on-failure      minio: -    image: minio/minio:RELEASE.2025-04-22T22-12-26Z +    image: minio/minio:RELEASE.2025-09-07T16-13-09Z      command: ["server", "/data", "--console-address", ":9001"]      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}      volumes:        - survey_minio_data:/data      networks: @@ -80,17 +82,17 @@ services:      deploy:        replicas: 1        restart_policy:          condition: on-failure      # Swarm ignores depends_on; retries are the fault-tolerance layer.    # Prefer Dokploy Compose (docker-compose.dokploy.yml) for migrate + init orchestration.    minio-init: -    image: minio/mc:RELEASE.2025-04-16T18-13-26Z +    image: minio/mc:RELEASE.2025-08-13T08-35-41Z      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?set MINIO_ROOT_PASSWORD}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}      entrypoint:        - /bin/sh        - -c        - | @@ -129,17 +131,17 @@ services:    api:      image: ${APP_IMAGE_API:-api-survey-api:prod}      env_file:        - ${API_ENV_FILE:-deploy/env/api.env}      environment:        NODE_ENV: production        PORT: "4000"        HOSTNAME: "0.0.0.0" -      REDIS_URL: redis://redis:6379 +      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379        MINIO_ENDPOINT: http://minio:9000        STORAGE_PROVIDER: minio      networks:        - survey-internal        - traefik-public      healthcheck:        test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4000/live"]        interval: 15s @@ -169,17 +171,17 @@ services:    worker:      image: ${APP_IMAGE_WORKER:-api-survey-worker:prod}      env_file:        - ${WORKER_ENV_FILE:-deploy/env/worker.env}      environment:        NODE_ENV: production        WORKER_PORT: "4001"        HOSTNAME: "0.0.0.0" -      REDIS_URL: redis://redis:6379 +      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379        MINIO_ENDPOINT: http://minio:9000        STORAGE_PROVIDER: minio      networks:        - survey-internal      healthcheck:        test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4001/live"]        interval: 20s        timeout: 5s diff --git a/docker-compose.dokploy.yml b/docker-compose.dokploy.yml index 1dac449..51fb95d 100644 --- a/docker-compose.dokploy.yml +++ b/docker-compose.dokploy.yml @@ -13,17 +13,17 @@  # See DEPLOYMENT.md and docs/ops/dokploy-env.md.  #  # Required secrets (no insecure defaults): POSTGRES_PASSWORD, REDIS_PASSWORD,  # MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, DATABASE_URL. Paste deploy/env/dokploy.compose.env.example.  #  # Domains (example):  #   admin.sdvedutech.in   ΓåÆ web:3000  #   backend.sdvedutech.in ΓåÆ api:4000 -# Host publish: web 3001ΓåÆ3000, api 4000, worker 4001 +# Traefik is the only ingress; no application ports are published on the host.  #  # Postgres 17 note: volumes created with Postgres 16 are not binary-compatible.  # Dump/restore or recreate the volume before first PG17 start.    services:    postgres:      image: postgres:17-alpine      restart: unless-stopped @@ -66,17 +66,17 @@ services:      deploy:        restart_policy:          condition: on-failure          delay: 5s          max_attempts: 10      minio:      # Pin a RELEASE tag; :latest may be scratch-based without curl/wget. -    image: minio/minio:RELEASE.2025-04-22T22-12-26Z +    image: minio/minio:RELEASE.2025-09-07T16-13-09Z      restart: unless-stopped      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:?Set MINIO_ROOT_USER in Dokploy Environment}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?Set MINIO_ROOT_PASSWORD in Dokploy Environment}      volumes:        - survey_minio_data_prod:/data      command: ["server", "/data", "--console-address", ":9001"]      # Image includes curl; health endpoint does not need `mc`. @@ -91,17 +91,17 @@ services:      deploy:        restart_policy:          condition: on-failure          delay: 5s          max_attempts: 10      minio-init:      # Pin mc RELEASE to match server era; :latest drifts and can break init. -    image: minio/mc:RELEASE.2025-04-16T18-13-26Z +    image: minio/mc:RELEASE.2025-08-13T08-35-41Z      depends_on:        minio:          condition: service_healthy      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:?Set MINIO_ROOT_USER in Dokploy Environment}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?Set MINIO_ROOT_PASSWORD in Dokploy Environment}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}      entrypoint: @@ -194,18 +194,16 @@ services:        DEMAND_NOTICE_PRINT_SECRET: ${DEMAND_NOTICE_PRINT_SECRET:-}        STORAGE_PROVIDER: minio        REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379        MINIO_ENDPOINT: http://minio:9000        MINIO_ROOT_USER: ${MINIO_ROOT_USER}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}        STORAGE_BUCKET: ${STORAGE_BUCKET:-api-survey-app} -    ports: -      - "4000:4000"      labels:        - traefik.enable=true        - traefik.docker.network=dokploy-network        - traefik.http.routers.survey-api.rule=Host(`${API_HOST:-backend.sdvedutech.in}`)        - traefik.http.routers.survey-api.entrypoints=websecure        - traefik.http.routers.survey-api.tls=true        - traefik.http.routers.survey-api.tls.certresolver=letsencrypt        - traefik.http.services.survey-api.loadbalancer.server.port=4000 @@ -258,18 +256,16 @@ services:        DIRECT_URL: ${DIRECT_URL:-${DATABASE_URL}}        STORAGE_PROVIDER: minio        REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379        MINIO_ENDPOINT: http://minio:9000        MINIO_ROOT_USER: ${MINIO_ROOT_USER}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}        STORAGE_BUCKET: ${STORAGE_BUCKET:-api-survey-app} -    ports: -      - "4001:4001"      healthcheck:        test: ["CMD", "wget", "-qO-", "http://127.0.0.1:4001/live"]        interval: 20s        timeout: 5s        retries: 12        start_period: 90s      networks:        - app @@ -302,19 +298,16 @@ services:          required: false      environment:        NODE_ENV: production        # Next must listen on 3000 inside the container (Traefik + healthcheck).        # Never set PORT=3001 here ΓÇö that yields Traefik "no available server".        PORT: "3000"        HOSTNAME: "0.0.0.0"        NEXT_TELEMETRY_DISABLED: "1" -    # Avoid host :3000 (Dokploy UI often binds it). Traefik uses container port 3000. -    ports: -      - "3001:3000"      labels:        - traefik.enable=true        - traefik.docker.network=dokploy-network        - traefik.http.routers.survey-web.rule=Host(`${WEB_HOST:-admin.sdvedutech.in}`)        - traefik.http.routers.survey-web.entrypoints=websecure        - traefik.http.routers.survey-web.tls=true        - traefik.http.routers.survey-web.tls.certresolver=letsencrypt        - traefik.http.services.survey-web.loadbalancer.server.port=3000 diff --git a/docker-compose.yml b/docker-compose.yml index bc649e4..dd375fd 100644 --- a/docker-compose.yml +++ b/docker-compose.yml @@ -29,17 +29,17 @@ services:      healthcheck:        test: ["CMD", "redis-cli", "ping"]        interval: 5s        timeout: 3s        retries: 10      minio:      # Pin RELEASE (same as prod); :latest may lack curl for healthchecks. -    image: minio/minio:RELEASE.2025-04-22T22-12-26Z +    image: minio/minio:RELEASE.2025-09-07T16-13-09Z      restart: unless-stopped      ports:        - "9000:9000"        - "9001:9001"      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}      volumes: @@ -48,17 +48,17 @@ services:      healthcheck:        test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]        interval: 5s        timeout: 5s        retries: 12        start_period: 10s      minio-init: -    image: minio/mc:RELEASE.2025-04-16T18-13-26Z +    image: minio/mc:RELEASE.2025-08-13T08-35-41Z      depends_on:        minio:          condition: service_healthy      environment:        MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}        MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}        MINIO_BUCKET: ${MINIO_BUCKET:-api-survey-app}      entrypoint: diff --git a/docs/ops/dokploy-compose-setup.md b/docs/ops/dokploy-compose-setup.md index bfbeef0..3156091 100644 --- a/docs/ops/dokploy-compose-setup.md +++ b/docs/ops/dokploy-compose-setup.md @@ -89,17 +89,17 @@ NODE_ENV=production    | Host                    | Service | Container port | Dokploy Domains tip                          |  | ----------------------- | ------- | -------------- | -------------------------------------------- |  | `admin.sdvedutech.in`   | `web`   | **3000**       | Service `web`, port **3000** (not host 3001) |  | `backend.sdvedutech.in` | `api`   | **4000**       | Service `api`, port **4000**                 |    Compose attaches `web`/`api` to external `dokploy-network` so Traefik can reach them. Prefer Domains in the Dokploy UI (redeploy after adding). Do not set Environment `PORT=3001` for web.   -Use container ports for Traefik/Dokploy domains (not host publish `3001`). +Traefik is the only ingress. Compose does not publish `web`, `api`, or `worker` ports on the host. Use the container ports above for Traefik/Dokploy domains; keep the worker internal.    ## 4. What Compose builds    | Service                        | Dockerfile               | Notes                                                     |  | ------------------------------ | ------------------------ | --------------------------------------------------------- |  | `migrate` + `api`              | `apps/api/Dockerfile`    | `turbo prune api --docker`; Prisma CLI linked for migrate |  | `worker`                       | `apps/worker/Dockerfile` | Playwright Chromium; longer first build                   |  | `web`                          | `apps/web/Dockerfile`    | Next standalone; needs `NEXT_PUBLIC_*` at build           | diff --git a/docs/ops/go-live.md b/docs/ops/go-live.md index f5c4323..819f7ac 100644 --- a/docs/ops/go-live.md +++ b/docs/ops/go-live.md @@ -10,37 +10,22 @@  ## Blockers only you can clear    1. **Infra + app secrets** in Dokploy Environment UI       Paste [`dokploy.compose.env.example`](../../deploy/env/dokploy.compose.env.example).       Interpolation-required: `POSTGRES_PASSWORD`, `REDIS_PASSWORD`, `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `DATABASE_URL`.       Runtime: Clerk, `NEXT_PUBLIC_*`, `CORS_ORIGIN` / `APP_URL` (host `postgres` in DB URLs).    2. **DNS + TLS in Dokploy** -   - `admin.sdvedutech.in` ΓåÆ service `web` container port **3000** (host map is `3001ΓåÆ3000`) +   - `admin.sdvedutech.in` ΓåÆ service `web` container port **3000**     - `backend.sdvedutech.in` ΓåÆ service `api` container port **4000** -   - Worker listens on `4001` (internal; open SG if you need direct access) +   - Worker listens on `4001` and remains internal     - Clerk allowed origins / redirect URLs include `https://admin.sdvedutech.in`   -3. **EC2 security group** ΓÇö allow inbound TCP `3001`, `4000`, `4001` (plus `80`/`443` for the proxy): - -```powershell -# Configure AWS credentials first, then: -.\scripts\ops\open-dokploy-ports.ps1 -``` - -Or manually: - -```bash -aws ec2 authorize-security-group-ingress --group-id sg-XXXXXXXX --protocol tcp --port 3001 --cidr 0.0.0.0/0 --region ap-south-1 -aws ec2 authorize-security-group-ingress --group-id sg-XXXXXXXX --protocol tcp --port 4000 --cidr 0.0.0.0/0 --region ap-south-1 -aws ec2 authorize-security-group-ingress --group-id sg-XXXXXXXX --protocol tcp --port 4001 --cidr 0.0.0.0/0 --region ap-south-1 -``` - -(Previous Dokploy host SG was `sg-02d37ca2cd71ed334` on `13.127.204.141`.) +3. **EC2 security group** ΓÇö expose only TCP `80`/`443` for Traefik. Do not allow public inbound access to application ports `3001`, `4000`, or `4001`.    ## Dokploy deploy steps    0. **App type must be Docker Compose** ΓÇö if you see `open Dockerfile: no such file`, follow [`dokploy-compose-setup.md`](./dokploy-compose-setup.md). Do **not** add a root Dockerfile.  1. Paste [`deploy/env/dokploy.compose.env.example`](../../deploy/env/dokploy.compose.env.example) into Dokploy **Environment** (replace all `REPLACE_ME_*`). Missing `MINIO_ROOT_USER` / `POSTGRES_PASSWORD` / etc. fails compose interpolation before start.  2. Confirm `DATABASE_URL` host is `postgres` (not localhost). Deploy Compose file `docker-compose.dokploy.yml`, context = repo root.  3. Wait for `postgres` / `minio` / `redis` healthy, `migrate` success (migrate also runs **catalog seed**: roles, permissions, reference catalogs, sample Etah geo), then `api` `/health` + `/ready`, then `web`.  4. Set `BOOTSTRAP_ADMIN_CLERK_USER_IDS` to your Clerk user id (`user_ΓÇª` from Clerk Dashboard ΓåÆ Users) **before or right after** first sign-in, then redeploy/restart **api**.
````
