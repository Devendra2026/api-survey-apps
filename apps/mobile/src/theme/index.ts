import { colors, radius, spacing, typography } from "@/theme/tokens";

export const theme = {
  colors,
  radius,
  spacing,
  typography,
  controlHeight: 48,
} as const;

export { colors, radius, spacing, typography };
